import { type Request, type Response, type NextFunction } from 'express';

export const requireAuth = (_req: Request, _res: Response, _next: NextFunction): void => {
  // TODO: implement in Step 2 (auth)
};
