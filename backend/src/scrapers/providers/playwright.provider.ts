import type { SiteConfig, ScraperResult } from '@tracker/shared';

export const scrapeWithPlaywright = async (
  _config: SiteConfig,
  _query: string,
): Promise<ScraperResult[]> => {
  // TODO: Step 1 — generic Playwright browser automation
  return [];
};
